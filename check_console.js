// This would show console messages if we had access to the browser console
// For now, the error message is visible in the UI: 
// "✗ API key test failed: exception TypeError: Failed to fetch sending request"
console.log("Console messages are captured in LoadingFX component");
